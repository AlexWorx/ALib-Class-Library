// =================================================================================================
// Instantiation of TMonoAllocator for PoolAllocatorHA and its inner type
// detail::MAFields<PoolAllocatorHA>.
// =================================================================================================

// Under Windows OS, we cannot do the following within the unit tests project, which imports the
// ALib as a DLL. Instead, with Windows OS, it has to be done in an external lib.
#if !defined(_WIN32)

#include "alib/monomem/monoallocator.t.hpp"

namespace alib::monomem {  template class TMonoAllocator<alib::PoolAllocatorHA>;  }

#endif // !defined(_WIN32)

//DOX_MARKER([ALIB_MANUAL_T_HPP_FILE_USAGE_IMPL])
